#pragma once

/// <summary>
/// Class that declares and tracks the position of both the grid and the individual blocks
/// </summary>
class Position {
public:
	Position(int row, int column);
		int row;
		int column;
};